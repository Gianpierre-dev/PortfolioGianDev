// Sistema de movimiento del sitio: un solo easing para toda la coreografía.
// Curva agresiva (arranca rápido, frena en seco), inspirada en easings tipo "hop".
export const EASE_SITE = [0.9, 0, 0.1, 1] as const;

export const DURATION_REVEAL = 0.9;
export const STAGGER_LINES = 0.12;
